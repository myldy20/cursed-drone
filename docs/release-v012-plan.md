# v0.12 release integration

The public release branch starts from current `main` and must preserve both verified installation targets:

- TrimUI Brick with Knulli / PortMaster;
- TrimUI Brick with NextUI.

It will integrate Guided Workflow, musical actors, Scala tuning, Euclidean events, modulation, Reverse Grains, Master FX and eight memory slots, while fixing actor mute in the audio graph and replacing all stale 0.9 controls and installation text.
