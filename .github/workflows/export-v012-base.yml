name: export-v012-base

on:
  pull_request:
    branches: [main]
    paths:
      - .github/workflows/export-v012-base.yml

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Export source tree
        run: |
          rm -rf .git
          tar -czf /tmp/cursed-drone-v012-base.tar.gz .
      - uses: actions/upload-artifact@v4
        with:
          name: cursed-drone-v012-base-source
          path: /tmp/cursed-drone-v012-base.tar.gz
          if-no-files-found: error
